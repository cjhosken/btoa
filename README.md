# Building Blender To Arnold

Run the build.sh command!

./build.sh BLENDER_VERSION ARNOLD_VERSION

eg:
`build.sh 5.0 7.4.4.0`



Windows is currently unsupported due to the fact that it's looking for a usd_ms.lib (when it needs a usd_ms.dll)